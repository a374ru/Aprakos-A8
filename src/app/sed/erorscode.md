# Ошибки и проблемы кода компонента `seed`

e001 - **Ошбка возврата**
e002 - **Ошбка ???**
---------

Описание:
---------
Ранее для проверки високосного года метод `vg()` вызывалась в операторе `if()` и срабатывал неправильно. Отныне возрат метода `vg()` присваивается переменной `v_year`. 
